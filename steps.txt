1) git init - инициализируем папку 
2) git add . - после создания новой папки, изменеий в проекте, добваляем измения 
3) git commit -m 'first commit' - создаем коммит, 'first commit' - название комита

4) На сайте github
  Create new ...
  New repository
  ...
  Create repository

5) git branch -M main - перейи в ветку main
6) git remote add origin https://github.com/FedoRich-P/test.git - связываем папку локальную с гитом.



git config --global user.name 'FedoRich-P'
git config --global user.name 'panchev_fedor@mail.ru'
git config --global core.autocrlf true
git config --global core.safecrlf warn

git status - проверка паок